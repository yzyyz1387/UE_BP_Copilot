# Optional. Default: 8787
PORT=8787

# Optional. Comma-separated list of browser origins allowed to call this local proxy.
# Keep the production site and localhost dev addresses.
ALLOWED_ORIGINS=https://ue-bp-copilot.vercel.app,http://localhost:5173,http://127.0.0.1:5173
